import React, { useEffect } from 'react';
import styled from 'styled-components';
import connect from 'react-redux';

interface WorksProp {}

const Works = (props: WorksProp) => {
    useEffect(() => {
        //load api
    }, []);
    return <></>;
};

const mapStatesToProps = (state: any) => {

    
};
